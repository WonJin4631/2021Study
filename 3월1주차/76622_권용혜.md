# 이중 우선순위 큐

```
#include <iostream>
#include <queue>
using namespace std;
int main()
{
    int t;
    cin >> t;

    for(int i=0; i<t; i++)
    {
        int k;
        cin >> k;

        queue<int> q;
        for(int j=0; j<k; j++)
        {
           char op1;
           int op2;
           
           cin >> op1 >> op2;

            if(op1 == 'D')
            {
                if(op2 == 1)
                {

                }
                else if(op2 == -1)
                {

                }
            }
            else
            {

            }
        }
    }

    return 0;
}
```
시간부족으로 끝까지 풀지못했습니다.
